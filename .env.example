# ==========================================
# ROCKSOUL WEB Environment Configuration
# ==========================================

# Optional: URL of deployed or local rocksoul-aws API backend
# Default fallback: degraded mode (degraded screen without fabricated data)
VITE_AWS_API_URL=http://localhost:3000

# Optional: URL of deployed or local rocksoul-correlation API backend
# Default fallback: same-origin or reviewed corpus fallback from rocksoul-correlation/main
VITE_CORRELATION_API_URL=http://localhost:8787
